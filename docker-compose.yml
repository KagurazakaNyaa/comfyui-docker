services:
  cpu:
    image: kagurazakanyaa/comfyui:cpu
    build:
      context: .
      dockerfile: cpu.dockerfile
      network: host
    ports:
      - 8188:8188/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data:/data

  rocm:
    image: kagurazakanyaa/comfyui:rocm
    build:
      context: .
      dockerfile: rocm.dockerfile
      network: host
    devices:
      - /dev/kfd
      - /dev/dri
    security_opt:
      - seccomp:unconfined
    ports:
      - 8188:8188/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data:/data

  therock:
    image: kagurazakanyaa/comfyui:therock
    build:
      context: .
      dockerfile: therock.dockerfile
      network: host
    devices:
      - /dev/kfd
      - /dev/dri
    security_opt:
      - seccomp:unconfined
    ports:
      - 8188:8188/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data:/data

  cu130:
    image: kagurazakanyaa/comfyui:cu130
    build:
      context: .
      dockerfile: cu130.dockerfile
      network: host
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ "gpu" ]
              driver: nvidia
              device_ids: [ "0" ]
    ports:
      - 8188:8188/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data:/data

  cu128:
    image: kagurazakanyaa/comfyui:cu128
    build:
      context: .
      dockerfile: cu128.dockerfile
      network: host
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ "gpu" ]
              driver: nvidia
              device_ids: [ "0" ]
    ports:
      - 8188:8188/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data:/data

  cu126:
    image: kagurazakanyaa/comfyui:cu126
    build:
      context: .
      dockerfile: cu126.dockerfile
      network: host
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ "gpu" ]
              driver: nvidia
              device_ids: [ "0" ]
    ports:
      - 8188:8188/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data:/data
